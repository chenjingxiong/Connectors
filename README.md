# StockSharp Connectors

This repository contains all the open connectors for StockSharp (S#).

They are open for modification and for adding new ones — feel free to change
the existing connectors or contribute your own.

## Sub-projects

> **Note:** `Common` directory is not a sub-project (no `.csproj`).

- [ActivFinancial](ActivFinancial/) - The **ACTIV Financial connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Aerodrome](Aerodrome/) - The **Aerodrome Finance connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Aevo](Aevo/) - The **Aevo connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [AlgoSeek](AlgoSeek/) - The **AlgoSeek connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [AliceBlue](AliceBlue/) - The **Alice Blue ANT API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Alor](Alor/) - The **Alor connector** connects StockSharp to a broker or trading service for Russian markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [AlorHistory](AlorHistory/) - The **AlorHistory connector** connects StockSharp to a source of Russian exchange and market data. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Alpaca](Alpaca/) - The **Alpaca connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [AlphaVantage](AlphaVantage/) - The **AlphaVantage connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [AltCoinTrader](AltCoinTrader/) - The **AltCoinTrader connector** integrates StockSharp with the South African AltCoinTrader spot market. Its ZAR-denominated order books make it useful for local price discovery, market monitoring, data collection, and automated crypto trading.
- [Amberdata](Amberdata/) - The **Amberdata connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Anchorage](Anchorage/) - The **Anchorage Digital connector** connects StockSharp to a digital-asset custody, settlement, or institutional service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [AngelOne](AngelOne/) - The **Angel One SmartAPI connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ApexOmni](ApexOmni/) - The **Apex Omni connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [AscendEx](AscendEx/) - The **AscendEX connector** integrates StockSharp with the published AscendEX Pro API. It covers cash spot, margin, and perpetual futures markets through one adapter.
- [Aster](Aster/) - The **Aster connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Avantis](Avantis/) - The **Avantis connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [B3Up2Data](B3Up2Data/) - The **B3 UP2DATA connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BTCMarkets](BTCMarkets/) - The **BTC Markets connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BTSE](BTSE/) - The **BTSE connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BYDFi](BYDFi/) - The **BYDFi connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Backpack](Backpack/) - The **Backpack Exchange connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Balancer](Balancer/) - The **Balancer connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BarChart](BarChart/) - The **Barchart connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bavest](Bavest/) - The **Bavest connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bcs](Bcs/) - The **BCS connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Benzinga](Benzinga/) - The **Benzinga connector** connects StockSharp to a financial news and event-data service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bibox](Bibox/) - The **Bibox connector** connects StockSharp to a legacy digital-asset exchange integration. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BigOne](BigOne/) - The **BigONE connector** connects StockSharp to BigONE spot and contract markets, providing one adapter for crypto pairs and coin- or USDT-margined derivatives.
- [Bigul](Bigul/) - The **Bigul connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Binance](Binance/) - The **Binance connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BinanceHistory](BinanceHistory/) - The **Binance Historical Data connector** imports public Binance market-data archives into StockSharp and normalizes them for storage, analysis, and replay.
- [BingX](BingX/) - The **BingX connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Birdeye](Birdeye/) - The **Birdeye connector** integrates StockSharp with Birdeye's on-chain cryptocurrency data APIs. It provides token discovery, current market indicators, and OHLCV history for a selected blockchain, with Solana used by default.
- [Bit2Me](Bit2Me/) - The **Bit2Me connector** connects StockSharp to Bit2Me Pro, the spot trading platform of the Spanish digital-asset provider. It is useful for systems that need direct access to crypto markets with EUR liquidity through the standard StockSharp message model.
- [BitFlyer](BitFlyer/) - The **bitFlyer Lightning connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BitGo](BitGo/) - The **BitGo connector** connects StockSharp to a digital-asset custody, settlement, or institutional service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BitStamp](BitStamp/) - The **Bitstamp connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitalong](Bitalong/) - The **Bitalong connector** connects StockSharp to a legacy digital-asset exchange integration. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitbank](Bitbank/) - The **Bitbank connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitexbook](Bitexbook/) - The **Bitexbook connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitfinex](Bitfinex/) - The **Bitfinex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitget](Bitget/) - The **Bitget connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bithumb](Bithumb/) - The **Bithumb connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitkub](Bitkub/) - The **Bitkub connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitmart](Bitmart/) - The **Bitmart connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitmex](Bitmex/) - The **Bitmex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BitoPro](BitoPro/) - The **BitoPro connector** connects StockSharp to BitoPro, a regulated Taiwan-focused cryptocurrency exchange with active TWD spot markets.
- [BitpandaFusion](BitpandaFusion/) - The **Bitpanda Fusion connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitrue](Bitrue/) - The **Bitrue connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitso](Bitso/) - The **Bitso connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bittrex](Bittrex/) - The **Bittrex connector** connects StockSharp to a legacy digital-asset exchange integration. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitunix](Bitunix/) - The **Bitunix connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bitvavo](Bitvavo/) - The **Bitvavo connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BloFin](BloFin/) - The **BloFin connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bloomberg](Bloomberg/) - The **Bloomberg connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bluefin](Bluefin/) - The **Bluefin connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Bmll](Bmll/) - The **BMLL connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Breeze](Breeze/) - The **ICICI Direct Breeze API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [BtcTurk](BtcTurk/) - The **BtcTurk connector** connects StockSharp to BtcTurk Kripto, a Turkish spot cryptocurrency exchange supporting TRY, BTC, USDT, and other markets.
- [Btce](Btce/) - The **BTC-e connector** connects StockSharp to a legacy digital-asset exchange integration. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Buda](Buda/) - The **Buda connector** integrates StockSharp with the Buda.com spot cryptocurrency exchange. Public market data is available without credentials, while authenticated REST operations use an API key and secret.
- [Bullish](Bullish/) - The **Bullish connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ByBit](ByBit/) - The **Bybit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ByBitHistory](ByBitHistory/) - The **Bybit Historical Data connector** imports public Bybit market-data archives into StockSharp and normalizes spot and derivative data for analysis and backtesting.
- [CQG](CQG/) - The **CQG Web API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CSV](CSV/) - The **CSV connector** connects StockSharp to a configurable data source. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CTP](CTP/) - The **CTP connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CapitalCom](CapitalCom/) - The **Capital.com connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CapitalFutures](CapitalFutures/) - The **Capital Futures API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CboeDataShop](CboeDataShop/) - The **Cboe DataShop / LiveVol connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Cetus](Cetus/) - The **Cetus connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Cex](Cex/) - The **CEX.IO connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Chainflip](Chainflip/) - The **Chainflip connector** integrates StockSharp with the Chainflip cross-chain liquidity network. It combines public State Chain and swap-service data with optional wallet configuration for submitting cross-chain swaps through the StockSharp transaction model.
- [ChainlinkDataStreams](ChainlinkDataStreams/) - The **Chainlink Data Streams connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ChoiceFinX](ChoiceFinX/) - The **Choice FinX connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinApi](CoinApi/) - The **CoinAPI connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinCap](CoinCap/) - The **CoinCap connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinCatch](CoinCatch/) - The **CoinCatch connector** integrates StockSharp with CoinCatch spot and derivatives markets. A product setting selects spot, USDT-margined futures, or coin-margined futures, while REST and WebSocket APIs provide market data and authenticated trading.
- [CoinDCX](CoinDCX/) - The **CoinDCX connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinEx](CoinEx/) - The **CoinEx connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinGecko](CoinGecko/) - The **CoinGecko connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinGlass](CoinGlass/) - The **CoinGlass connector** integrates StockSharp with the CoinGlass cryptocurrency market-analytics API. It maps selected futures, spot, options, Bitcoin ETF, and Ethereum ETF datasets to StockSharp instruments, Level 1 messages, and historical candles.
- [CoinJar](CoinJar/) - The **CoinJar Exchange connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinMarketCap](CoinMarketCap/) - The **CoinMarketCap connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinMetrics](CoinMetrics/) - The **Coin Metrics connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinPaprika](CoinPaprika/) - The **CoinPaprika connector** integrates StockSharp with the CoinPaprika cryptocurrency market-data API. It exposes global coin reference data or markets from a selected exchange, together with ticker snapshots and historical OHLCV candles.
- [CoinSpot](CoinSpot/) - The **CoinSpot connector** integrates StockSharp with the CoinSpot cryptocurrency spot exchange and broker. It uses CoinSpot's public, trading, and read-only REST APIs for market data, account state, and order operations.
- [CoinSwitch](CoinSwitch/) - The **CoinSwitch connector** integrates StockSharp with the CoinSwitch PRO APIs. A product setting selects INR or USDT spot markets, USDT-margined perpetual futures, or the private-beta HFT options surface.
- [CoinTR](CoinTR/) - The **CoinTR connector** connects StockSharp to CoinTR, a cryptocurrency exchange focused on the Turkish market, with spot instruments available through the standard StockSharp message model.
- [CoinW](CoinW/) - The **CoinW connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Coinalyze](Coinalyze/) - The **Coinalyze connector** integrates StockSharp with the Coinalyze cryptocurrency market-analytics API. It maps historical price and derivatives indicators to standard StockSharp time-frame candles for futures or spot instruments.
- [Coinbase](Coinbase/) - The **Coinbase connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Coincall](Coincall/) - The **Coincall connector** integrates StockSharp with Coincall options and futures. A product setting selects the derivatives surface, while REST provides snapshots and history and authenticated WebSocket sessions provide live market and private updates.
- [Coincheck](Coincheck/) - The **Coincheck connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Coinhako](Coinhako/) - The **Coinhako connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Coinigy](Coinigy/) - The **Coinigy connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Coinmetro](Coinmetro/) - The **Coinmetro connector** integrates StockSharp with the Coinmetro cryptocurrency spot exchange. It combines REST instrument, account, order, and candle endpoints with WebSocket updates for live market and private activity, and supports separate live and demo environments.
- [Coinone](Coinone/) - The **Coinone connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CoinsPh](CoinsPh/) - The **Coins.ph connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Coinstore](Coinstore/) - The **Coinstore connector** connects StockSharp to Coinstore's spot cryptocurrency market, supporting broad listing monitoring and crypto-to-stablecoin trading.
- [Comdirect](Comdirect/) - The **comdirect connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Copper](Copper/) - The **Copper connector** connects StockSharp to a digital-asset custody, settlement, or institutional service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CowProtocol](CowProtocol/) - The **CoW Protocol connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CryptoCom](CryptoCom/) - The **Crypto.com connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [CryptoQuant](CryptoQuant/) - The **CryptoQuant connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Curve](Curve/) - The **Curve connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [DXtrade](DXtrade/) - The **DXtrade connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Daishin](Daishin/) - The **Daishin CYBOS Plus connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Databento](Databento/) - The **Databento connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [DeepBook](DeepBook/) - The **DeepBook connector** integrates StockSharp with the DeepBook liquidity protocol on Sui. It combines the public DeepBook indexer with a Sui full-node gRPC endpoint for pool data, wallet balances, and locally signed immediate swaps.
- [Deepcoin](Deepcoin/) - The **Deepcoin connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Definedge](Definedge/) - The **Definedge connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [DeltaExchangeIndia](DeltaExchangeIndia/) - The **Delta Exchange India connector** connects StockSharp to an Indian centralized digital-asset derivatives venue. It maps futures and options market data, orders, and account state to the standard StockSharp message model.
- [Deribit](Deribit/) - The **Deribit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Deriv](Deriv/) - The **Deriv connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [DexScreener](DexScreener/) - The **DexScreener connector** brings multi-chain decentralized-exchange pair analytics into StockSharp through DexScreener's public REST API. It is a read-only market-data adapter and does not require API credentials.
- [Dexalot](Dexalot/) - The **Dexalot connector** connects StockSharp to Dexalot's on-chain central limit order book on the Avalanche-based Dexalot L1. It combines public REST and WebSocket data with EVM contract calls for spot trading and account state.
- [Dhan](Dhan/) - The **DhanHQ v2 connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Digifinex](Digifinex/) - The **Digifinex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Directa](Directa/) - The **Directa connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Dnse](Dnse/) - The **DNSE connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [DowJones](DowJones/) - The **Dow Jones connector** connects StockSharp to a financial news and event-data service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Drift](Drift/) - The **Drift connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [DukasCopyJForex](DukasCopyJForex/) - The **Dukascopy JForex connector** connects StockSharp to Dukascopy Bank through the official Java JForex SDK and a local loopback bridge.
- [DxFeed](DxFeed/) - The **dxFeed connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [DydxChain](DydxChain/) - The **dYdX Chain connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ETrade](ETrade/) - The **E*TRADE connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Edinet](Edinet/) - The **EDINET connector** connects StockSharp to a financial-data and reference-information service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different data sources.
- [EodHistoricalData](EodHistoricalData/) - The **EOD Historical Data connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [EsmaFirds](EsmaFirds/) - The **ESMA FIRDS connector** connects StockSharp to a financial-data and reference-information service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different data sources.
- [Etoro](Etoro/) - The **eToro connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [EuronextWebServices](EuronextWebServices/) - The **Euronext Web Services connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Exante](Exante/) - The **EXANTE connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Exegy](Exegy/) - The **Exegy connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Exmo](Exmo/) - The **Exmo connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Extended](Extended/) - The **Extended connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [FTX](FTX/) - The **FTX connector** connects StockSharp to a legacy digital-asset exchange integration. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [FXOpen](FXOpen/) - The **FXOpen TickTrader connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [FactSet](FactSet/) - The **FactSet Prices connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [FalconX](FalconX/) - The **FalconX connector** connects StockSharp to a digital-asset custody, settlement, or institutional service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Finage](Finage/) - The **Finage connector** connects StockSharp to Finage's Forex market-data services. It is a read-only adapter for currency instruments, combining REST reference and historical data with an optional WebSocket quote stream.
- [FinamTrade](FinamTrade/) - The **Finam Trade API connector** connects StockSharp applications to Finam brokerage accounts and market data, normalizing instruments, quotes, orders, trades, and portfolio state.
- [FinancialDatasets](FinancialDatasets/) - The **Financial Datasets connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [FinancialModelingPrep](FinancialModelingPrep/) - The **Financial Modeling Prep connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Finnhub](Finnhub/) - The **Finnhub connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Finra](Finra/) - The **FINRA connector** connects StockSharp to a financial-data and reference-information service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different data sources.
- [Fireblocks](Fireblocks/) - The **Fireblocks connector** connects StockSharp to a digital-asset custody, settlement, or institutional service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Firstock](Firstock/) - The **Firstock connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [FivePaisa](FivePaisa/) - The **5paisa connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Fix](Fix/) - The **FIX Protocol connector** connects StockSharp to brokers, exchanges, and trading systems through configurable FIX sessions and maps dialect-specific messages to the unified model.
- [Flattrade](Flattrade/) - The **Flattrade Pi API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [FluidDex](FluidDex/) - The **Fluid DEX connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Foxbit](Foxbit/) - The **Foxbit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [FubonNeo](FubonNeo/) - The **Fubon Neo API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Fugle](Fugle/) - The **Fugle Market Data API connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Fxcm](Fxcm/) - The **FXCM connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Fyers](Fyers/) - The **FYERS API v3 connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [GMTrade](GMTrade/) - The **GMTrade connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [GainsNetwork](GainsNetwork/) - The **Gains Network gTrade connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [GateIO](GateIO/) - The **Gate.io connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [GateIOHistory](GateIOHistory/) - The **Gate.io Historical Data connector** imports public Gate.io market-data archives into StockSharp for storage, analysis, and replay.
- [Gemini](Gemini/) - The **Gemini connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Glassnode](Glassnode/) - The **Glassnode connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Gleif](Gleif/) - The **GLEIF connector** connects StockSharp to a financial-data and reference-information service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different data sources.
- [GmoCoin](GmoCoin/) - The **GMO Coin connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Gmx](Gmx/) - The **GMX connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Gopax](Gopax/) - The **Gopax connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Groww](Groww/) - The **Groww Trading API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Grvt](Grvt/) - The **GRVT connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [GuruFocus](GuruFocus/) - The **GuruFocus connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [HashKey](HashKey/) - The **HashKey Global connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [HdfcSecurities](HdfcSecurities/) - The **HDFC Securities connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [HitBtc](HitBtc/) - The **HitBTC connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Huobi](Huobi/) - The **Huobi connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Hyperliquid](Hyperliquid/) - The **Hyperliquid connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [IG](IG/) - The **IG Markets connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [IIFL](IIFL/) - The **IIFL connector** connects StockSharp to the IIFL Markets Open API for Indian exchange market data and brokerage operations. It maps IIFL REST and MQTT services to the standard StockSharp message model.
- [IQFeed](IQFeed/) - The **IQFeed connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [IndependentReserve](IndependentReserve/) - The **Independent Reserve connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Indodax](Indodax/) - The **Indodax connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Injective](Injective/) - The **Injective connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [InteractiveBrokers](InteractiveBrokers/) - The **Interactive Brokers connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Intrinio](Intrinio/) - The **Intrinio connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [InvertirOnline](InvertirOnline/) - The **InvertirOnline connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [JQuants](JQuants/) - The **J-Quants connector** connects StockSharp to the J-Quants API V2 for Japanese market reference and historical data. It is a read-only REST adapter intended for research rather than live market-data streaming or trading.
- [Jainam](Jainam/) - The **Jainam connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [JpmDataQuery](JpmDataQuery/) - The **J.P. Morgan DataQuery connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [JpxTdnet](JpxTdnet/) - The **JPX TDnet connector** connects StockSharp to a financial-data and reference-information service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different data sources.
- [Jupiter](Jupiter/) - The **Jupiter connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [KabuStation](KabuStation/) - The **Mitsubishi UFJ eSmart kabu Station API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Kaiko](Kaiko/) - The **Kaiko connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Kalshi](Kalshi/) - The **Kalshi connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Kiwoom](Kiwoom/) - The **Kiwoom REST API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Korbit](Korbit/) - The **Korbit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [KoreaInvestment](KoreaInvestment/) - The **Korea Investment & Securities Open API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [KoreanFsc](KoreanFsc/) - The **Korean FSC connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [KotakNeo](KotakNeo/) - The **Kotak Neo Trade API v2 connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Kraken](Kraken/) - The **Kraken connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [KrxOpenApi](KrxOpenApi/) - The **KRX Open API connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Kucoin](Kucoin/) - The **Kucoin connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [KucoinHistory](KucoinHistory/) - The **KuCoin Historical Data connector** imports public KuCoin market-data archives into StockSharp and normalizes spot and futures data.
- [KyberSwap](KyberSwap/) - The **KyberSwap connector** connects StockSharp to KyberSwap Aggregator API v1 and EVM networks. It exposes configured token pairs as StockSharp instruments, derives executable quotes from aggregator routes, and submits signed on-chain swaps.
- [LBank](LBank/) - The **LBank connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [LCX](LCX/) - The **LCX connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [LMAX](LMAX/) - The **LMAX connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Latoken](Latoken/) - The **Latoken connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [LemonMarkets](LemonMarkets/) - The **lemon.markets connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Lfj](Lfj/) - The **LFJ connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Ligther](Ligther/) - The **Ligther connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Lime](Lime/) - The **Lime Trader connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Longbridge](Longbridge/) - The **Longbridge OpenAPI connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [LsSecurities](LsSecurities/) - The **LS Securities Open API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [LsegRealTime](LsegRealTime/) - The **LSEG Real-Time connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Luno](Luno/) - The **Luno Exchange connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MStock](MStock/) - The **m.Stock connector** connects StockSharp to an Indian broker and its supported exchange segments. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ManifestTrade](ManifestTrade/) - The **Manifest Trade connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MarketDataApp](MarketDataApp/) - The **MarketData.app connector** connects StockSharp to a professional market-data service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same requests and workflows across different data sources.
- [Marketaux](Marketaux/) - The **Marketaux connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Marketstack](Marketstack/) - The **Marketstack connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Marquee](Marquee/) - The **Goldman Sachs Marquee connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MasterLink](MasterLink/) - The **MasterLink connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Mastertrust](Mastertrust/) - The **Mastertrust connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MatchTrader](MatchTrader/) - The **Match-Trader connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MaxExchange](MaxExchange/) - The **MAX Exchange connector** connects StockSharp to the Taiwan-focused spot exchange operated by MaiCoin Group, especially for TWD and USDT markets.
- [MercadoBitcoin](MercadoBitcoin/) - The **Mercado Bitcoin connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MetaApi](MetaApi/) - The **MetaApi connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Meteora](Meteora/) - The **Meteora DLMM connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Mexc](Mexc/) - The **Mexc connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MiraeSharekhan](MiraeSharekhan/) - The **Mirae Asset Sharekhan connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MoexISS](MoexISS/) - The **MOEX ISS connector** connects StockSharp to a source of Russian exchange and market data. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MoexLchi](MoexLchi/) - The **MOEX LCHI connector** connects StockSharp to a source of Russian exchange and market data. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Moomoo](Moomoo/) - The **Moomoo connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Morningstar](Morningstar/) - The **Morningstar Direct Web Services connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MotilalOswal](MotilalOswal/) - The **Motilal Oswal MO API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [MtNewswires](MtNewswires/) - The **MT Newswires connector** connects StockSharp to a financial news and event-data service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [NDAX](NDAX/) - The **NDAX connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Nado](Nado/) - The **Nado connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [NasdaqCloudDataService](NasdaqCloudDataService/) - The **Nasdaq Cloud Data Service connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [NasdaqDataLink](NasdaqDataLink/) - The **Nasdaq Data Link connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [NinjaTrader](NinjaTrader/) - The **NinjaTrader connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Noren](Noren/) - `StockSharp.Noren` provides the shared Noren protocol implementation used by provider-specific connectors; it is not a standalone connector.
- [NovaDax](NovaDax/) - The **NovaDAX connector** integrates StockSharp with NovaDAX's spot cryptocurrency market, focusing on Brazilian-real pairs.
- [Nubra](Nubra/) - The **Nubra connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Nuvama](Nuvama/) - The **Nuvama connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [OSL](OSL/) - The **OSL Global connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Oanda](Oanda/) - The **OANDA connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Okex](Okex/) - The **Okex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [OkexHistory](OkexHistory/) - The **OkexHistory connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [OneInch](OneInch/) - The **1inch connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [OpenDart](OpenDart/) - The **Open DART connector** connects StockSharp to a financial-data and reference-information service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different data sources.
- [OpenFigi](OpenFigi/) - The **OpenFIGI connector** connects StockSharp to a financial-instrument identifier mapping and reference-data service. It translates provider-specific results into the unified StockSharp security model, so applications can use consistent instrument identifiers across different data sources.
- [OpenMarkets](OpenMarkets/) - The **OpenMarkets connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [OptionMetrics](OptionMetrics/) - The **OptionMetrics IvyDB connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Orats](Orats/) - The **ORATS connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Orca](Orca/) - The **Orca Whirlpools connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [OrderlyNetwork](OrderlyNetwork/) - The **Orderly Network connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Osmosis](Osmosis/) - The **Osmosis connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Ostium](Ostium/) - The **Ostium connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Ourbit](Ourbit/) - The **Ourbit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Pacifica](Pacifica/) - The **Pacifica connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [PancakeSwap](PancakeSwap/) - The **PancakeSwap connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Paradex](Paradex/) - The **Paradex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Paxos](Paxos/) - The **Paxos connector** connects StockSharp to a digital-asset custody, settlement, or institutional service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [PaytmMoney](PaytmMoney/) - The **Paytm Money connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Pendle](Pendle/) - The **Pendle connector** connects StockSharp to an on-chain yield-trading protocol. It translates protocol data and wallet operations into the unified StockSharp message model, so applications can use standard subscriptions and transaction workflows for Pendle markets.
- [Phemex](Phemex/) - The **Phemex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [PhillipPoems](PhillipPoems/) - The **Phillip POEMS connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [PintuPro](PintuPro/) - The **Pintu Pro connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Pionex](Pionex/) - The **Pionex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Poloniex](Poloniex/) - The **Poloniex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [PolygonIO](PolygonIO/) - The **Polygon.io connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Polymarket](Polymarket/) - The **Polymarket CLOB connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Ppi](Ppi/) - The **PPI connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Primary](Primary/) - The **Primary connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [PrizmBit](PrizmBit/) - The **PrizmBit connector** connects StockSharp to a legacy digital-asset exchange integration. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ProBit](ProBit/) - The **ProBit Global connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Public](Public/) - The **Public.com connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [PumpSwap](PumpSwap/) - The **PumpSwap connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Pyth](Pyth/) - The **Pyth Pro connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [QFEX](QFEX/) - The **QFEX connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Qmt](Qmt/) - The **QMT connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [QuantFeed](QuantFeed/) - The **QuantHouse QuantFEED connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Questrade](Questrade/) - The **Questrade API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [QuickSwap](QuickSwap/) - The **QuickSwap connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Quidax](Quidax/) - The **Quidax connector** integrates StockSharp with the Quidax spot exchange, including crypto markets quoted in NGN and other African fiat currencies.
- [QuiverQuant](QuiverQuant/) - The **Quiver Quantitative connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Quodd](Quodd/) - The **QUODD connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Rain](Rain/) - The **Rain connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [RakutenRss](RakutenRss/) - The **Rakuten RSS connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [RavenPack](RavenPack/) - The **RavenPack connector** connects StockSharp to a financial news and event-data service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Raydium](Raydium/) - The **Raydium connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Reya](Reya/) - The **Reya connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Rithmic](Rithmic/) - The **Rithmic connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Robinhood](Robinhood/) - The **Robinhood connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Rss](Rss/) - The **RSS connector** connects StockSharp to a financial news and event-data service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Rupeezy](Rupeezy/) - The **Rupeezy connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [SSI](SSI/) - The **SSI connector** connects StockSharp to SSI FastConnect API v3 for the Vietnamese securities market. It translates SSI market data and brokerage operations into the unified StockSharp message model.
- [Samco](Samco/) - The **Samco connector** connects StockSharp to the Samco Trade API for Indian securities and derivatives. It exposes the broker's market-data and trading services through the unified StockSharp message model.
- [Saxo](Saxo/) - The **Saxo OpenAPI connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Schwab](Schwab/) - The **Charles Schwab connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [SecApi](SecApi/) - The **SEC API connector** connects StockSharp to a financial-data and reference-information service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different data sources.
- [SecEdgar](SecEdgar/) - The **SEC EDGAR connector** gives StockSharp read-only access to official United States Securities and Exchange Commission filing data. It maps issuers, filings, and XBRL company facts to StockSharp securities, news, and a dedicated fundamental-data message type.
- [SetMarketData](SetMarketData/) - The **SET Market Data connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Settrade](Settrade/) - The **Settrade connector** connects StockSharp to Settrade Open API v2 for Thai equities and derivatives. It unifies the provider's REST and MQTT market-data and brokerage services under the StockSharp message model.
- [Shioaji](Shioaji/) - The **SinoPac Shioaji connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Shoonya](Shoonya/) - The **Shoonya API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [SierraChartDtc](SierraChartDtc/) - The **Sierra Chart DTC connector** connects StockSharp to a trading or market-data protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [SimFin](SimFin/) - The **SimFin connector** gives StockSharp read-only access to SimFin company fundamentals and daily price history. It maps provider records to StockSharp securities, Level 1 snapshots, daily candles, and a dedicated fundamental-data message type.
- [SnapTrade](SnapTrade/) - The **SnapTrade connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [SpGlobal](SpGlobal/) - The **S&P Global connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [StandX](StandX/) - The **StandX connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [StockDataOrg](StockDataOrg/) - The **StockData.org connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [StocksTrader](StocksTrader/) - The **StocksTrader connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [StonFi](StonFi/) - The **STON.fi connector** connects StockSharp to STON.fi liquidity pools and the TON blockchain. It represents configured or discovered pools as instruments and translates swap quotes, pool events, wallet balances, and submitted swaps into StockSharp messages.
- [SunIo](SunIo/) - The **SUN.io connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Swissquote](Swissquote/) - The **Swissquote connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [SynFutures](SynFutures/) - The **SynFutures connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Synthetix](Synthetix/) - The **Synthetix connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [THORChain](THORChain/) - The **THORChain connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Talos](Talos/) - The **Talos connector** connects StockSharp to a digital-asset custody, settlement, or institutional service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tapbit](Tapbit/) - The **Tapbit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tardis](Tardis/) - The **Tardis.dev connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TaseDataHub](TaseDataHub/) - The **TASE Data Hub connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TastyTrade](TastyTrade/) - The **tastytrade connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tests](Tests/) - The connector test project contains automated tests and shared harnesses for validating StockSharp connector behavior.
- [ThetaData](ThetaData/) - The **ThetaData connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TigerBrokers](TigerBrokers/) - The **Tiger Brokers OpenAPI connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tiingo](Tiingo/) - The **Tiingo connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tinkoff](Tinkoff/) - The **Tinkoff connector** connects StockSharp to a broker or trading service for Russian markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tokocrypto](Tokocrypto/) - The **Tokocrypto connector** connects StockSharp to Tokocrypto's MAIN spot market for Indonesian-focused cryptocurrency trading and market data.
- [Toobit](Toobit/) - The **Toobit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TossSecurities](TossSecurities/) - The **Toss Securities connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tpex](Tpex/) - The **TPEx connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TradeLocker](TradeLocker/) - The **TradeLocker connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TradeOgre](TradeOgre/) - The **TradeOgre connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TradeStation](TradeStation/) - The **TradeStation connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TradeZero](TradeZero/) - The **TradeZero connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tradejini](Tradejini/) - The **Tradejini connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TraderMade](TraderMade/) - The **TraderMade connector** connects StockSharp to TraderMade's foreign-exchange and cryptocurrency market-data services. It maps REST history and WebSocket quotes to the unified StockSharp market-data model.
- [Tradernet](Tradernet/) - The **Tradernet connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tradier](Tradier/) - The **Tradier connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Trading212](Trading212/) - The **Trading 212 connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TradingEconomics](TradingEconomics/) - The **Trading Economics connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [TradingTechnologies](TradingTechnologies/) - The **Trading Technologies connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Tradovate](Tradovate/) - The **Tradovate connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Transaq](Transaq/) - The **Transaq connector** connects StockSharp to a Transaq trading server used by Russian brokers, converting its XML commands and asynchronous updates into StockSharp messages.
- [TwelveData](TwelveData/) - The **Twelve Data connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Twse](Twse/) - The **TWSE connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Uniswap](Uniswap/) - The **Uniswap connector** connects StockSharp to an on-chain trading and liquidity protocol. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [UnusualWhales](UnusualWhales/) - The **Unusual Whales connector** connects StockSharp to a professional market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Upbit](Upbit/) - The **Upbit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Upstox](Upstox/) - The **Upstox V3 connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Usmart](Usmart/) - The **uSMART connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [VALR](VALR/) - The **VALR connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [VariationalOmni](VariationalOmni/) - The **Variational Omni connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [VeloData](VeloData/) - The **Velo Data connector** connects StockSharp to a digital-asset market-data and analytics service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Velodrome](Velodrome/) - The **Velodrome connector** connects StockSharp to Velodrome classic and Slipstream liquidity pools on Optimism. It maps configured pools, executable quotes, on-chain swaps, wallet balances, and submitted transactions to StockSharp messages.
- [Velora](Velora/) - The **Velora connector** connects StockSharp to the Velora Market API and supported EVM networks. It represents configured token pairs as instruments and maps executable prices, wallet balances, and routed swaps to StockSharp messages.
- [Ventura](Ventura/) - The **Ventura connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [WazirX](WazirX/) - The **WazirX connector** connects StockSharp to the WazirX centralized cryptocurrency spot exchange. It translates exchange REST and WebSocket data and account operations into the unified StockSharp message model.
- [Webull](Webull/) - The **Webull connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Weex](Weex/) - The **WEEX connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [WhiteBit](WhiteBit/) - The **WhiteBIT connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [WisdomCapital](WisdomCapital/) - The **Wisdom Capital connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [WooX](WooX/) - The **WOO X connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [XOpenHub](XOpenHub/) - The **X Open Hub connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [XbrlFilings](XbrlFilings/) - The **XBRL Filings connector** connects StockSharp to a financial-data and reference-information service. It translates provider-specific data into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different data sources.
- [Xrpl](Xrpl/) - The **XRPL connector** connects StockSharp to the decentralized exchange built into the XRP Ledger. It maps configured currency pairs, ledger order books, executed offers, account balances, and signed transactions to StockSharp messages.
- [Xt](Xt/) - The **XT.COM connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Xtp](Xtp/) - The **XTP connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Yobit](Yobit/) - The **Yobit connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Yuanta](Yuanta/) - The **Yuanta SPARK API connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ZB](ZB/) - The **ZB connector** connects StockSharp to a legacy digital-asset exchange integration. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Zaif](Zaif/) - The **Zaif connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [Zebu](Zebu/) - The **Zebu connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ZeroHash](ZeroHash/) - The **Zero Hash connector** connects StockSharp to a digital-asset custody, settlement, or institutional service. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ZeroX](ZeroX/) - The **0x connector** connects StockSharp to 0x Swap API v2 and supported EVM networks. It represents configured token pairs as instruments and maps executable prices, wallet balances, and routed swaps to StockSharp messages.
- [Zerodha](Zerodha/) - The **Zerodha connector** connects StockSharp to a broker or electronic venue for financial markets. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [ZondaCrypto](ZondaCrypto/) - The **Zonda Crypto connector** connects StockSharp to the zondacrypto centralized cryptocurrency spot exchange. It translates exchange REST and WebSocket data and account operations into the unified StockSharp message model.
- [Zoomex](Zoomex/) - The **Zoomex connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [cTrader](cTrader/) - The **cTrader connector** connects StockSharp to an FX/CFD broker or trading platform. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.
- [edgeX](edgeX/) - The **edgeX connector** connects StockSharp to a centralized digital-asset exchange. It translates provider-specific data and operations into the unified StockSharp message model, so applications can use the same subscriptions and workflows across different venues.

## Documentation

See the [StockSharp documentation](https://doc.stocksharp.com/en/topics/api/connectors/creating_own_connector.html)
on how to create your own connector.

## Support

Questions and support: [chat](https://stocksharp.com/en/chat/).
